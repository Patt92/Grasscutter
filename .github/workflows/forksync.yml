name: Sync Fork

on:
  schedule:
    - cron: '0 * * * *' # every hour
  workflow_dispatch: # on button click

jobs:
  sync:

    runs-on: ubuntu-latest

    steps:
      - uses: tgymnich/fork-sync@v1.4
        with:
          owner: Grasscutter
          base: development
          head: 17808b49037f182758a6cc723e1fa3bbd9416dd9
          token: ${{ secrets.PERSONAL_TOKEN }}
